#!/bin/bash

maim() {
	command maim --hidecursor $@ 
	(( $? != 0 )) && exit 1
}

if [[ "$1" == "-s" ]]; then 
	shift
	OPTS='-s -b 3 -c 0.600,0.306,0.333'
fi

# FILE=${1:-'/tmp/temp.png'}

cd ~/scrots
maim --format jpg $OPTS
echo $FILE
