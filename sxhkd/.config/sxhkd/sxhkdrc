super + shift + Return
	termite -e fish

Print
	~/bin/imgur $(~/bin/img)

shift + @Print
	~/bin/imgur $(~/bin/img -s)

super + p
	~/bin/dmenu2

super + shift + p
	chromium

super + shift + e
	emacsclient -c --no-wait

# make sxhkd reload its configuration files:
ctrl + alt + r
	pkill -USR1 -x sxhkd

# vim: set syntax=sh:
