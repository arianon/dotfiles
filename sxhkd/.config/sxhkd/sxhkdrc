# i don't know why the fuck do I have to unset $TMUX
super + Return
	unset TMUX; urxvt

Print
	~/bin/imgur

shift + @Print
	~/bin/imgur -s

super + p
	~/bin/dmenu2

super + w
	firefox

# make sxhkd reload its configuration files:
ctrl + alt + r
	pkill -USR1 -x sxhkd
